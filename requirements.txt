pytest>=8.0
pypdf>=4.0
python-docx>=1.1
python-pptx>=0.6
requests>=2.31
beautifulsoup4>=4.12

# Busca semantica (opcional, desligada por omissao): so e precisa para
# construir e usar o indice de embeddings. O motor arranca sem elas.
# numpy>=2.0
# fastembed>=0.8
