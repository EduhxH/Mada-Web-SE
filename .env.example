# Copie para .env quando puser o servidor no ar.
# Se esta variavel existir, e usada em vez de data/segredo.txt.
# Gere uma com: python -c "import secrets; print(secrets.token_hex(32))"
MADALENA_SEGREDO=
