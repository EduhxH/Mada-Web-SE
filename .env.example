# Copie para .env e preencha. O .env esta no .gitignore e NUNCA e versionado.

# --- Moodle: sincronizacao dos materiais das suas disciplinas ---
MOODLE_URL=https://moodle.sefo.pt
MOODLE_UTILIZADOR=
MOODLE_SENHA=

# --- Chave de assinatura das sessoes ---
# Se existir, e usada em vez de data/segredo.txt.
# Gere uma com: python -c "import secrets; print(secrets.token_hex(32))"
MADALENA_SEGREDO=
